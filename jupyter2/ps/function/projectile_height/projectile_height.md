# <font color="green">Projectile Height</font>

* Write a function `height` that takes initial height $h_0$ (meters), initial vertical velocity $v_0$ (m/s), and time $t$ (seconds), all 64-bit floating point numbers, and returns the height of the mass point.  That is,

$$ {{\rm height}}(h_0, v_0, t) = h_0 + v_0 t - \frac{{1}}{{2}}gt^2 $$

* Use $g = 9.8$.

* Boilerplate source files `{{go,jl,ml,rs}}/{base}.{{go,jl,ml,rs}}` containing the test code is generated and shown below.

* Edit the source files either by opening them in a text editor (e.g., vscode), or editing the cells below and executing them.
