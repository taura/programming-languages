# <font color="green">Calling convention (where are args and return values?)</font>

* investigate how integer parameters are passed in your languages
* to this end, define a function `many_args` (or `manyArgs`) that takes many (> 10) integer parameters and returns the sum of them (similar to the following C function) in your language, compile it into assembly and examine it

# Problem

Make a note about the following points

1. how are parameters passed in the language? which registers? memory?
1. how is the return value passed in the language? which registers? memory?
1. discuss differences between languages in the team and write notable difference(s) you found

