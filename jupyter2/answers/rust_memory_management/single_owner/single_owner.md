# <font color="green">Single Owner</font>

* Rust maintains a that there is one and only one single owner pointer to each live data

* Rust compiler tries to reject code that violates it at compile-time

* Observe the compiler does the job
