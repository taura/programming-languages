# <font color="green"> ... </font>

* Just as any reference type that appears in function parameters or return type must have a lifetime parameter (like `&'a T`) explicitly, so must reference types in structure fields


