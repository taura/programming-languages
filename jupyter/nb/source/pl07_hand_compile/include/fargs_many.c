double add_many(double a, double b, double c, double d, double e, double f,
                double g, double h, double i, double j, double k, double l,
                double m, double n, double o, double p, double q, double r) {
  double x = 0.0;
  x += a;
  x += b;
  x += c;
  x += d;
  x += e;
  x += f;
  x += g;
  x += h;
  x += i;
  x += j;
  x += k;
  x += l;
  x += m;
  x += n;
  x += o;
  x += p;
  x += q;
  x += r;
  return x;
}
