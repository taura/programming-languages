long add_many(long a, long b, long c, long d, long e, long f,
              long g, long h, long i, long j, long k, long l,
              long m, long n, long o, long p, long q, long r) {
  long x = 0;
  x += a;
  x += b;
  x += c;
  x += d;
  x += e;
  x += f;
  x += g;
  x += h;
  x += i;
  x += j;
  x += k;
  x += l;
  x += m;
  x += n;
  x += o;
  x += p;
  x += q;
  x += r;
  return x;
}
