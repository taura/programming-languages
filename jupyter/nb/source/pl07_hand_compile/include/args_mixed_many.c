double gg(double * A, double a, double * B, double b, double * C, double c,
          double * D, double d, double * E, double e, double * F, double f,
          double * G, double g, double * H, double h, double * I, double i,
          double * J, double j, double * K, double k, double * L, double l,
          double * M, double m, double * N, double n, double * O, double o,
          double * P, double p, double * Q, double q, double * R, double r) {
  double x = 0.0;
  x += A[0]; x += a;
  x += B[0]; x += b;
  x += C[0]; x += c;
  x += D[0]; x += d;
  x += E[0]; x += e;
  x += F[0]; x += f;
  x += G[0]; x += g;
  x += H[0]; x += h;
  x += I[0]; x += i;
  x += J[0]; x += j;
  x += K[0]; x += k;
  x += L[0]; x += l;
  x += M[0]; x += m;
  x += N[0]; x += n;
  x += O[0]; x += o;
  x += P[0]; x += p;
  x += Q[0]; x += q;
  x += R[0]; x += r;
  return x;
}
